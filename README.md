**DermaGuide** — персональний навігатор, що аналізує склад косметики та дає індивідуальні рекомендації.

## Команда проєкту

- **Коробка Анастасія** — розробник/дизайнер
- **Дрозд Софія** — розробник/дизайнер

## Технології

**Frontend:**
- HTML5, CSS3, Vanilla JavaScript
- **Flexbox** — гнучкі макети, навігація, вирівнювання
- **CSS Grid** — сітки продуктів, картки рекомендацій
- **Медіазапити** — адаптивний дизайн (1200px, 968px, 768px, 600px, 480px, 360px)
- **CSS Modules** — base.css, layout.css, components.css, style.css
- **CSS Transitions** — плавні переходи для кнопок та інтерактивних елементів
- **LocalStorage** – збереження налаштувань теми та розміру шрифту
- **DOM API** – динамічна маніпуляція вмістом

## Структура проєкту

```
DermaGuide/
├── index.html              # Головна сторінка
├── auth.html               # Авторизація та опитування
├── analysis.html           # Сканування інгредієнтів
├── recommendations.html    # Рекомендації продуктів
├── product.html            # Сторінка продукту 1
├── product2.html           # Сторінка продукту 2
├── css/                    # Стилі
│   ├── base.css
│   ├── layout.css
│   ├── components.css
│   └── style.css
├── js/
│   └── main.js             # Основний JavaScript файл
├── images/                 # Зображення
└── README.md               # Опис проєкту
```

## Функціонал

- Сканування інгредієнтів косметики
- Персоналізовані рекомендації
- Анкета для визначення типу шкіри
- База безпечних косметичних засобів

## JavaScript Функціонал

### 📋 DOM Маніпуляції
- **Динамічне додавання елементів:** Створення акордеону "Показати більше" та дисклеймера через `createElement()` та `append()`
- **Зміна стилів:** Підсвітка елементів `.benefit-text` при наведенні миші
- **Оновлення контенту:** Автоматичне відображення поточної дати у футері

### Events

```javascript
// Обробка кліків
document.getElementById('theme-toggle').addEventListener('click', function() {
    document.body.classList.toggle('dark-theme');
});

// Обробка наведення миші
menuItem.addEventListener('mouseenter', function() {
    this.classList.add('menu-highlight');
});

// Обробка клавіатури
document.addEventListener('keydown', function(e) {
    if (e.ctrlKey && e.key === 'ArrowUp') {
        // Збільшення розміру шрифту
    }
});
```

### Валідація Форм

```javascript
// Валідація email
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
if (!emailRegex.test(email)) {
    showError('email', 'Введіть коректний email');
}

// Валідація довжини поля
if (name.length < 3) {
    showError('name', 'Імʼя повинно містити щонайменше 3 символи');
}

// Перевірка radio buttons
const skinType = document.querySelector('input[name="skin"]:checked');
if (!skinType) {
    showSurveyError('Оберіть тип шкіри');
}
```

### LocalStorage

```javascript
// Збереження теми
localStorage.setItem('dermaGuide-theme', 'dark');

// Отримання теми
const savedTheme = localStorage.getItem('dermaGuide-theme');
if (savedTheme === 'dark') {
    document.body.classList.add('dark-theme');
}

// Збереження даних форми
const formData = {
    name: document.getElementById('name').value,
    skinType: document.querySelector('input[name="skin"]:checked').value
};
localStorage.setItem('userData', JSON.stringify(formData));
```

### DOM API

```javascript
// Створення та додавання елементів
const element = document.createElement('div');
document.querySelector('main').appendChild(element);

// Маніпуляція класами
element.classList.add('dark-theme');
element.classList.remove('hidden');

// Пошук елементів
const benefitTexts = document.querySelectorAll('.benefit-text');
```
